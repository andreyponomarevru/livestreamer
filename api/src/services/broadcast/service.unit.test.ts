import { describe, it } from "@jest/globals";

describe("broadcastService", () => {
  describe("read", () => {
    it.todo("returns the broadcast by id");
  });

  describe("readAllPublished", () => {
    it.todo("returns all published broadcasts");
  });

  describe("readAllHidden:", () => {
    it.todo("retirns all hidden broadcasts");
  });

  describe("updatePublished", () => {
    it.todo("updates the published broadcast");
  });

  describe("updateHidden", () => {
    it.todo("updates the hidden broadcast");
  });

  describe("destroy", () => {
    it.todo("deletes the broadcast by id");
  });

  describe("create", () => {
    it.todo("saves and returns the scheduled broadcast");
  });

  describe("destroy", () => {
    it.todo("deletes the scheduled broadcast");
  });
});
